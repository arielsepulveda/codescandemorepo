# Pinned to known-vulnerable versions on purpose so the dependency
# advisory side of the demo has something to chew on. Every dependency
# scanner (Dependabot, Snyk, Trivy) catches these — included because we
# don't want CodeScan to be seen as missing the obvious dep-CVE class.
Flask==1.0.0          # multiple post-1.0 fixes
PyJWT==1.6.0          # CVE-2017-11424 (alg=none confusion)
